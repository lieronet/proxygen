@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label class="control-label">@Model.Cards</label>
            <textarea asp-for="Input" cols="50" rows="30" class="form-control" placeholder="Murder
Jace, the Mind Sculptor
7 Seven Dwarves
Word of Command"></textarea>
        </div>
        <div class="form-group">
            <input type="submit" formaction="Index" value="Make!" class="btn btn-primary"/>
        </div>
    </form>
</div>
